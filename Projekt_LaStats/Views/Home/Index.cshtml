@model IEnumerable<Projekt_LaStats.Models.League>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome at LaStats</h1>
    
    @foreach(var item in Model)
    {
        <div>
            <a asp-action="InfoLeague" asp-controller="Info" asp-route-id="@item.Id">
                <h2>@Html.DisplayFor(modelItem => item.Name)</h2>
            </a>
            </div>
    }
</div>
